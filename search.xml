<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>JVM</title>
      <link href="/JVM/"/>
      <url>/JVM/</url>
      
        <content type="html"><![CDATA[<h2 id="1-自动内存管理"><a href="#1-自动内存管理" class="headerlink" title="1 自动内存管理"></a>1 自动内存管理</h2><h3 id="1-1-运行时数据区"><a href="#1-1-运行时数据区" class="headerlink" title="1.1 运行时数据区"></a>1.1 运行时数据区</h3><blockquote><p><img src="/JVM/image-20230329161904157.png" alt="image-20230329161904157"></p></blockquote><h4 id="1-1-1-程序计数器"><a href="#1-1-1-程序计数器" class="headerlink" title="1.1.1 程序计数器"></a>1.1.1 程序计数器</h4><ol><li>一块较小的内存空间</li><li>当前线程所执行的字节码的行号指示器，字节码解释器工作时就是通过改变这个计数器的值来选取下一条需要执行的字节码指令</li><li>因为多线程间涉及到线程的切换，为了线程切换后能恢复到正确的执行位置，每条线程都需要有一个独立的程序计数器，各条线程之间计数器互不影响，独立存储，这块内存区域为”线程私有”内存。</li><li>如果线程执行的是一个Java方法，则程序计数器中记录的是当前执行的字节码指令的地址；如果执行的是一个Native方法，则计数器的值为空(Undefined)</li><li>唯一一个没有规定任何OutOfMemoryError的区域</li></ol><h4 id="1-1-2-Java虚拟机栈"><a href="#1-1-2-Java虚拟机栈" class="headerlink" title="1.1.2 Java虚拟机栈"></a>1.1.2 Java虚拟机栈</h4><ol><li>也是线程私有的，生命周期与线程相同。</li><li>当每个方法被执行时，JVM都会创建一个栈帧，存储局部变量表、操作数栈、动态连接、方法出口等信息</li><li>如果线程请求的栈深度大于虚拟机允许的深度，则抛出StackOverflowError；如果Java虚拟机栈的容量可以动态扩展，则当栈扩展到无法申请足够内存时抛出OutOfMemoryError。</li></ol><h5 id="1-1-2-1-局部变量表"><a href="#1-1-2-1-局部变量表" class="headerlink" title="1.1.2.1 局部变量表"></a>1.1.2.1 局部变量表</h5><ol><li>存放了编译器可知的基本数据类型(boolean、byte、char、short、int、float、long、double)、对象引用(reference类型，它并不等同于对象本身，可能是一个指向对象起始地址的引用指针，也可能是指向一个代表对象的句柄或者其他与此对象相关的位置)、 returnAddress类型（指向了一条字节码指令的地址）</li><li>以上的数据类型在局部变量表中的存储空间是以局部变量槽(slot)为单位的，除long类型和double类型会占用两个slot外，其余类型都只会占用一个slot。局部变量表所需要的空间在编译期间就已经确认，其中slot的个数在编译期间确定，而每个slot占用的内存空间有JVM的实现确定。</li></ol><h4 id="1-1-3-本地方法栈"><a href="#1-1-3-本地方法栈" class="headerlink" title="1.1.3 本地方法栈"></a>1.1.3 本地方法栈</h4><ol><li>对于本地方法栈中方法使用的语言、使用方式与数据结构并没有任何强制规定。</li><li>如果线程请求的栈深度大于虚拟机允许的深度，则抛出StackOverflowError；如果Java虚拟机栈的容量可以动态扩展，则当栈扩展到无法申请足够内存时抛出OutOfMemoryError。</li></ol><h4 id="1-1-4-Java堆"><a href="#1-1-4-Java堆" class="headerlink" title="1.1.4 Java堆"></a>1.1.4 Java堆</h4><ol><li>几乎所有的对象实例都在堆上分配内存</li><li>“新生代”“老年代”“永久代”“Eden 空间”“From Survivor 空间”“To Survivor 空间”等名词仅仅是一种经典分代，在多年前的HotSpot虚拟机中的所有垃圾收集器都采用的这种分代，但时至今日已经有很多不采用分代设计的新的垃圾收集器。</li><li>可以处于物理上不连续的内存空间中，但在逻辑上是连续的。</li><li>Java 堆中没有内存完成实例分配，并且堆也无法再扩展时，Java 虚拟机将会抛出 OutOfMemoryError</li></ol><h4 id="1-1-5-方法区"><a href="#1-1-5-方法区" class="headerlink" title="1.1.5 方法区"></a>1.1.5 方法区</h4><ol><li>线程共享的内存区域。用于存储已被虚拟机加载的类型信息、常量、静态变量、即时编译器编译后的代码缓存等数据。</li><li>虽然《JVM规范》中将方法区描述为堆的一个逻辑部分，但是还是和堆区分开好。</li><li>关于方法区和永久代的问题，永久代是HotSpot中《JVM规范》关于方法区的实现，并非是一个等价概念。JDK7时HotSpot把原本存在永久代的字符串常量池、静态变量等移出，JDK8时废除永久代，使用在本地内存中实现的元空间（Metaspace）代替，并将JDK7中永久代中剩余内容全部移到元空间中。</li><li>如果方法区无法满足新的内存分配需求时，抛出OutOfMemoryError</li></ol><h5 id="1-1-5-1-运行时常量池"><a href="#1-1-5-1-运行时常量池" class="headerlink" title="1.1.5.1 运行时常量池"></a>1.1.5.1 运行时常量池</h5><ol><li>方法区的一部分，Class 文件中除了有类的版本、字段、方法、接口等描述信息外，还有一项信息是常量池表（Constant Pool Table），用于存放编译期生成的各种字面量与符号引用，这部分内容会在类加载后存储到运行时常量池中。</li><li>并不要求常量一定只有编译期才能产生，也就是说，并非预置入 Class 文件中常量池的内容才能进入方法区运行时常量池，运行期间也可以将新的常量放入池中，这种特性被开发人员利用得比较多的便是 String 类的 intern()方法。</li><li>当常量池无法再申请到内存时会抛出 OutOfMemoryError。</li></ol><h4 id="1-1-6-直接内存"><a href="#1-1-6-直接内存" class="headerlink" title="1.1.6 直接内存"></a>1.1.6 直接内存</h4><ol><li>比如NIO中DirectByteBuffer对象可以直接所以直接内存的引用进行操作。</li><li>当需要动态扩展时超出了本机总内存的大小时，抛出OutOfMemoryError。</li></ol><h3 id="1-2-HotSpot虚拟机中的对象"><a href="#1-2-HotSpot虚拟机中的对象" class="headerlink" title="1.2 HotSpot虚拟机中的对象"></a>1.2 HotSpot虚拟机中的对象</h3><h4 id="1-2-1-普通对象的创建（不包括数组和Class对象）"><a href="#1-2-1-普通对象的创建（不包括数组和Class对象）" class="headerlink" title="1.2.1 普通对象的创建（不包括数组和Class对象）"></a>1.2.1 普通对象的创建（不包括数组和Class对象）</h4><p><strong>普通对象的创建过程</strong></p><ol><li>JVM在遇到一条字节码new指令时，先检查该指令的参数能否在常量池中定义到一个类的符号引用，并检查该符号引用代表的类是否已经被加载、解析、初始化，如果没有，则必须先执行相应的类加载流程。</li><li>为新生对象分配内存。对象所需内存在类加载后即可完全确定，故只需要将一块确定大小的内存从堆中划分即可。这部分需要后续的垃圾回收详细了解。</li><li>内存分配完成之后，虚拟机必须将分配到的内存空间（但不包括对象头）都初始化为零值，如果使用了 TLAB （Thread Local Allocation Buffer，本地线程分配缓存）的话，这一项工作也可以提前至 TLAB 分配时顺便进行。</li><li>设置对象的对象头信息。</li></ol><h4 id="1-2-2-对象的内存布局"><a href="#1-2-2-对象的内存布局" class="headerlink" title="1.2.2 对象的内存布局"></a>1.2.2 对象的内存布局</h4><ol><li><p>在HotSpot中，对象在堆内存中的存储可以分为：对象头（Header）、实例数据（Instance Data）、对齐填充（Padding）</p></li><li><p>对象头分为运行时数据（MarkWord）、类型指针</p><ol><li><p>MarkWord</p><ol><li><p>存储HashCode、GC年龄分代、锁状态标志、线程持有的锁、偏向线程ID、偏向时间戳等，结构为BitMap</p></li><li><p>长度在 32 位和 64 位的虚拟机（未开启压缩指针）中分别为 32 个bit和 64 个bit</p></li><li><p>其中存储的信息及其所占大小随对象加锁状态变化而变化</p></li><li></li></ol><p>   <img src="/JVM/image-20230331150618069.png" alt="image-20230331150618069"></p></li><li><p>类型指针</p><ol><li>指向对象类型元数据的指针</li></ol></li></ol></li><li><p>对象中所有的各种类型的字段内容的存储顺序序会受到虚拟机分配策略参数（-XX：FieldsAllocationStyle 参数）和字段在 Java 源码中定义顺序的影响。HotSpot 虚拟机默认的分配顺序为 longs&#x2F;doubles、ints、shorts&#x2F;chars、bytes&#x2F;booleans、oops（Ordinary Object Pointers，OOPs），可以看出相同宽度的字段总是被分配到一起存放。如果 HotSpot 虚拟机的+XX：CompactFields 参数值为true（默认就为 true），那子类之中较窄的变量也允许插入父类变量的空隙之中，以节省出一点点空间。</p></li><li><p>对齐部分。HotSpot中要求对象的起始地址必须是8Byte的整数倍，故需要对齐。</p></li></ol><h4 id="1-2-3-对象的访问定位"><a href="#1-2-3-对象的访问定位" class="headerlink" title="1.2.3 对象的访问定位"></a>1.2.3 对象的访问定位</h4><ol><li><p>JVM会通过栈上栈帧中的reference数据来操作堆中具体对象。</p></li><li><p>reference数据的实现并没有规定，故主流的方式有两种：句柄、直接指针</p><ol><li><p>堆中可能划分出一块内存作为句柄池，reference中存储句柄地址，句柄中包含对象实例数据和类型数据各自的具体地址信息。</p><p><img src="/JVM/image-20230331152141425.png" alt="image-20230331152141425"></p></li><li><p>reference中存放的直接就是对象实例数据的指针</p><p><img src="/JVM/image-20230403150603395.png" alt="image-20230403150603395"></p></li><li><p>使用句柄来访问的最大好处就是 reference 中存储的是稳定句柄地址，在对象被移动（垃圾收集时移动对象是非常普遍的行为）时只会改变句柄中的实例数据指针，而 reference 本身不需要被修改。</p></li><li><p>使用直接指针来访问最大的好处就是速度更快，它节省了一次指针定位的时间开销。</p></li></ol></li></ol><h3 id="1-3-垃圾收集器和内存分配策略"><a href="#1-3-垃圾收集器和内存分配策略" class="headerlink" title="1.3 垃圾收集器和内存分配策略"></a>1.3 垃圾收集器和内存分配策略</h3><h4 id="1-3-1-标记算法"><a href="#1-3-1-标记算法" class="headerlink" title="1.3.1 标记算法"></a>1.3.1 标记算法</h4><h5 id="1-3-1-1-引用计数算法"><a href="#1-3-1-1-引用计数算法" class="headerlink" title="1.3.1.1 引用计数算法"></a>1.3.1.1 引用计数算法</h5><ol><li>在对象中添加一个引用计数器，每当有一个地方引用它时，计数器值就加一；当引用失效时，计数器值就减一；任何时刻计数器为零的对象就是不可能再被使用的。</li><li>无法解决两个对象相互引用的回收问题</li></ol><h5 id="1-3-1-2-可达性分析算法"><a href="#1-3-1-2-可达性分析算法" class="headerlink" title="1.3.1.2 可达性分析算法"></a>1.3.1.2 可达性分析算法</h5><ol><li>通过一系列称为“GC Roots”的根对象作为起始节点集，从这些节点开始，根据引用关系向下搜索，搜索过程所走过的路径称为“引用链”（Reference  Chain），如果某个对象到 GC Roots 间没有任何引用链相连，或者用图论的话来说就是从 GC Roots 到这个对象不可达时，则证明此对象是不可能再被使用的。</li><li>在Java中，固定可以作为GC Roots的对象如下：<ol><li>在方法区中类静态属性引用的对象</li><li>在方法区中常量引用的对象</li><li>本地方法栈中JNI（Native方法）引用的对象</li><li>JVM内部的引用，如基本数据类型的Class对象，常驻的异常对象，系统类加载器</li><li>所有被同步锁持有的对象</li><li>反映 Java 虚拟机内部情况的 JMXBean、JVMTI 中注册的回调、本地代码缓存等。</li></ol></li></ol><h4 id="1-3-2-标记-清除算法"><a href="#1-3-2-标记-清除算法" class="headerlink" title="1.3.2 标记-清除算法"></a>1.3.2 标记-清除算法</h4><ol><li>标记所有存活对象或者非存活对象，之后使用清除算法对所有未标记的或者已标记的对象进行回收</li><li>主要有两个缺点：一是执行效率不稳定。如果堆中有大量的对象，且其中大部分需要被回收，那么标记和清除两个过程都会变长，效率降低；二是在内存回收过后内存空间会碎片化，从而导致之后分配较大对象时无法找到连续的内存空间再一次触发内存回收。</li></ol><h4 id="1-3-3-标记-复制算法"><a href="#1-3-3-标记-复制算法" class="headerlink" title="1.3.3 标记-复制算法"></a>1.3.3 标记-复制算法</h4>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
